# Backend Configuration
FLASK_ENV=production
FLASK_DEBUG=0

# Frontend Configuration
# Use the backend API URL for the frontend (set by deployment platform)
# For Vercel: Set this in environment variables
VITE_API_URL=https://your-backend-url.render.com/api

# Backend Server Port (Render/Railway default: 10000)
PORT=5000
